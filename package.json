{
  "name": "fessior-oj-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "check": "turbo run check",
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@11.17.0",
  "devDependencies": {
    "turbo": "^2.10.7"
  }
}
